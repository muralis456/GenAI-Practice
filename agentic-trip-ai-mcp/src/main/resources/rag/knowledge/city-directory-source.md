# City Directory Source

The city/airport knowledge in this RAG corpus is derived from the project class `AirportLocationSeeder`. If that Java seed list changes, regenerate the RAG city documents so the knowledge base stays aligned with the application's airport resolver.
